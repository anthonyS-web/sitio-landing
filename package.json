{
  "name": "sitio-landing",
  "version": "1.0.0",
  "description": "Sitio landing page with Vercel Analytics",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required for static HTML'",
    "test": "echo 'No tests configured'"
  },
  "dependencies": {
    "@vercel/analytics": "^1.4.1"
  }
}
